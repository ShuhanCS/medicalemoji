# Kidney v2.8.0 Elephant Search and Video Plan

## Goal

Create a complete Kidney proposal packet that can truthfully answer **Yes** to the live Unicode form question
asking whether the proposal compares the proposed emoji with Elephant in all five required frequency sources.

## Audience

The proposal is written for Unicode emoji reviewers applying the live 2026 intake form. Reviewers must be able
to verify each query, source, capture date, and visible result count directly from the PDF.

## Required Correction

The live form explicitly names Google Search, Google Video Search, Google Trends Image Search, Google Trends Web
Search, and Google Books Ngram Viewer. Packet v2.7.0 compares Kidney with Elephant only in the two Trends
exhibits and Books Ngram. Packet v2.8.0 will add reproducible Elephant exhibits to Google Search and Google
Video Search and will report the Kidney-to-Elephant comparison without implying that raw indexed-result counts
measure quality or medical importance.

## Deliverables

- Preserve `submissions/v2.7.0/` unchanged as a historical review packet.
- Create `submissions/v2.8.0/` with the same approved author roster, paired-kidney artwork, and substantive
  selection-factor answers.
- Add signed-out Elephant Google Search and Google Video Search screenshots whose query and result count are
  visible.
- Update the proposal text, manifest, regression audit, and validation records so all five comparison methods
  are explicit and auditable.
- Regenerate and visually inspect the complete PDF; do not file, email, or submit it.

## Evidence Rules

1. Do not infer or fabricate a result count. The number must be visible in the captured Google interface.
2. Record the exact capture date, query URL, language setting, and lack of date/country filters.
3. Keep the screenshots large enough for a reviewer to read the query and count in the PDF.
4. Describe Search and Video counts as volatile approximate index estimates and use them only for the required
   relative comparison.
5. If Google blocks the capture with a CAPTCHA, use a user-visible browser handoff and resume only after the
   user clears it.

## Version Decision

Create packet `v2.8.0`. This is a MINOR packet change because it adds two required comparison exhibits and
changes the frequency-evidence argument. Bump the repository version from `0.31.0` to `0.32.0`.

## Validation

- Confirm the exact ten-person author order, Microsoft affiliations, and exclusion of Jarone Lee.
- Confirm four paired-kidney artwork images and all selection-factor sections remain present.
- Confirm all five required methods visibly compare Kidney with Elephant.
- Confirm the PDF embeds eleven images: four artwork images and seven frequency screenshots.
- Render every final page and inspect it for clipping, overlap, awkward page breaks, and unreadable evidence.
- Verify hyperlinks, page numbering, evidence filenames, screenshot provenance, and validation records.
- Run the renderer compile check, repository lint, and `git diff --check`.

## Filing Status

`NOT FILED - REVIEW REQUIRED`. Shuhan He must personally review the PDF, agree to the Unicode form terms, and
submit the form.
