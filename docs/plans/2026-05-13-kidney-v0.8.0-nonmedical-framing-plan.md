# Kidney v0.8.0 Non-Medical Framing Plan

Date: 2026-05-13

## Goal

Revise the planned kidney submission packet so the `Multiple Concepts` section is not overly medical or nephrology-focused. The proposal should still support the medical use case, but it should also show that `kidney` is an everyday concept with culinary, educational, shape-based, wellness, and metaphorical uses.

## Scope

- Create synchronized packet `submissions/v0.8.0/` from `submissions/v0.7.0/`.
- Keep proposal and required example images marked `SUBMIT`.
- Expand proposal keywords beyond clinical terms.
- Rewrite `A. Multiple Concepts` with non-medical and general-public uses.
- Add non-medical sequence examples.
- Update packet manifest, execution log, README, changelog, and project semver.

## Framing Guardrails

- Do not make `kidney bean` the primary justification; the proposed emoji remains an anatomical kidney.
- Acknowledge food/bean usage as an everyday language and shape-recognition bridge.
- Avoid claiming that the kidney emoji replaces the existing bean emoji for food.
- Keep public-health sources as supporting context, not the entire argument.
