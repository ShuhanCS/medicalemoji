# Kidney v2.9.0 Same-Day Evidence Plan

## Goal

Create the final Kidney review packet with methodologically matched, same-day Firefox captures for Kidney and
Elephant in Google Search and Google Video Search.

## Audience

The proposal is written for Unicode emoji reviewers applying the live 2026 submission form and frequency
guidance. Every required query, result count, setting, and comparison must be visible and reproducible.

## Evidence Method

1. Use Firefox Private Browsing for all four captures on 2026-07-28.
2. Search the exact proposed name `kidney` and comparison term `elephant`; do not combine related concepts or
   use broader Boolean searches.
3. Use English-language, signed-out results with no date or country filter selected.
4. Open Google's Tools menu so the approximate result count is visible.
5. Capture Google Search and Google Video Search for both terms in the same browser session.
6. Record whatever counts Google displays. Do not select a capture date or query variant because it produces a
   more favorable number.

## Packet Changes

- Preserve `submissions/v2.8.0/` unchanged.
- Create immutable packet `submissions/v2.9.0/`.
- Replace all four Search and Video screenshots and their counts with the same-day Firefox captures.
- Keep the existing Trends Web, Trends Image, and Books Ngram comparisons unchanged.
- Align the proposal's suggested keywords to the form's five-keyword maximum:
  `renal; dialysis; transplant; donation; stone`.
- Preserve the exact author order, Microsoft affiliations, paired-kidney artwork, selection-factor answers,
  and `NOT FILED` status.

## Version Decision

Create packet `v2.9.0`. This is a MINOR packet change because all Search and Video evidence is replaced and the
submission-facing identification fields are synchronized with the live form. Bump the repository version from
`0.32.0` to `0.33.0`.

## Validation

- Verify all four screenshots visibly show the query, search mode, Tools menu, and approximate result count.
- Confirm the Search and Video comparison dates and settings match.
- Confirm the exact ten-person author order and exclusion of Jarone Lee.
- Confirm the PDF contains four artwork images and seven frequency screenshots covering all five methods.
- Render and visually inspect every PDF page for clipping, overlap, broken links, and unreadable evidence.
- Verify dimensions, page numbering, HTTPS links, PDF SHA-256, and public logged-out accessibility.
- Run the renderer compile check, repository lint, and `git diff --check`.

## External Actions

After the packet is committed and pushed, upload the exact reviewed PDF as a new version of the existing public
Google Drive file if the authenticated Drive session can be verified. Confirm the logged-out download is
byte-identical before reporting the Drive URL as ready.

Do not submit the Unicode form or accept its legal agreement on Shuhan He's behalf.
